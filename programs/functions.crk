Program {

	func int mult(int valA, int valB){
		int result = valA;
		while(valB>1){
			result = result + valA;
			valB = valB - 1;
		}
		print(result);
		return result;
	}
	
	//print n multiples of mult//
	func int multiples(int n, int mult){
		int i = 0;
		while(i < (n+1)){
			mult(mult, i);
			i = i+1;
		}
		return 9;
	}

	
	main {
		multiples(3,3);
	}
	
}