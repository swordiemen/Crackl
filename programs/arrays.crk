Program{

    int[] arrA = [2,4,6,8]; //elements placed on the shared heap, arrA stored on local heap//

	main{
		int[3] b;
	    int[] arrB = [3,6,9]; //elements placed on the shared heap, arrB on stack//
		boolean boolVal = true;
		#boolean boolPtr => boolVal;
		
		int[3] c;
		arrA[2+1] = 7;
		
		b[2] = 1;

        int i = 0;
        while(i<4){
			print(arrA[i]);
			i=i+1;
        }

		i = 0;
        while(i<3){
			print(arrB[i]);
			i=i+1;
        }
	}
}
