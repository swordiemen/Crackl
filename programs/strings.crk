Program{

	func int wait(int ms){
		while(ms>0){
			ms = ms-1;
		}
		return 0;
	}

	main{
		text long = "";
		text dis = "===========";
		text hey = "hello world";

		out(dis);
		while(true){
			out(hey);
			wait(3000);
			out(dis);
			wait(4000);
		}
	}
}
